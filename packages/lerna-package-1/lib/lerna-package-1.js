'use strict';
const _ = require('lodash');
module.exports = lernaPackage1;

function lernaPackage1(str) {
    return _.toLower(str);
}

// something change
console.log('this is running in package 11ws swa');
